<Application x:Class="ClipboardUtility.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ClipboardUtility">
    <!-- StartupUri を削除（コードで作成するため） -->
    <Application.Resources>
        <FontFamily x:Key="GlobalFont">./Assets/Fonts/Roboto/static/Roboto_Condensed-Black.ttf#Roboto Condensed</FontFamily>
        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="{StaticResource GlobalFont}"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource GlobalFont}"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource GlobalFont}"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="{StaticResource GlobalFont}"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="FontFamily" Value="{StaticResource GlobalFont}"/>
        </Style>
    </Application.Resources>
</Application>